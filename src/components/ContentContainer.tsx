import { Container, styled } from "@mui/material";

const ContentContainer = styled(Container)`
  padding: 0px; /* Отступы по краям */
  margin-top: 0px; /* Отступ сверху */
`;

export default ContentContainer;
